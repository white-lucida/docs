# 通常処理

この項目では、接続後の処理内容について記述します。

## 挙動

### *text-in-voice* のリッスン
[接続したボイスチャンネル](./ready)の*text-in-voice*でメッセージをリッスンします。
それ以外のチャンネルで送信されたメッセージは利用しません。

### 音声合成
*OpenJTalk* による音声合成を行います。

### キューに音声を追加する
[キュー](./queue)に音声データを追加します。

## テスト

- 単体テストの必要は必要ありません。

